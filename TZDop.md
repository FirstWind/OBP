Отличные вопросы — это как раз тот уровень детализации, который **нужно зафиксировать в ТЗ**, иначе потом система “упрётся”. Отвечаю по пунктам и сразу даю **как правильно реализовать**.

---

## 1. Изменение возрастной группы по медицинским показателям — **ДА, нужно**, но аккуратно

Важно разделить **факт** и **расчёт**.

### Правильная модель (рекомендую)

У человека в сессии должно быть **ТРИ значения**, а не одно:

### 1️⃣ Базовая возрастная группа (нормативная)

* вычисляется автоматически;
* **никогда не меняется вручную**;
* используется как источник истины.

```text
age_group_base   — вычислено по дате рождения и 31.12
```

Пример:
34 года → `age_group_base = 3 (30–34)`

---

### 2️⃣ Медицинская корректировка возрастной группы

Это **не категория**, а именно **коррекция возрастной группы**.

```text
age_group_med_delta   — INTEGER, может быть:
  -1, 0, +1 (рекомендую ограничить диапазон)
age_group_med_reason  — VARCHAR (обязательно)
age_group_med_source  — manual / medical_commission
```

Пример:

* базовая группа = 3
* по медпоказаниям “облегчили” → **группа 4**
* значит:

```text
age_group_med_delta = +1
effective_age_group = 4
```

⚠️ Почему **delta**, а не “просто выбрать 4”:

* всегда видно, **от чего оттолкнулись**;
* легко проверить корректность;
* исключает “поставили с потолка”.

---

### 3️⃣ Эффективная возрастная группа (используется в расчётах)

```text
age_group_effective = age_group_base + age_group_med_delta
```

Правила:

* `age_group_effective` **используется во всех расчётах** (norms, шкалы, пороги);
* при выходе за допустимый диапазон → ошибка `AGE_GROUP_OUT_OF_RANGE`.

---

### Как это выглядит в UI (важно)

В карточке участника сессии:

```
Возрастная группа:
  базовая:        3 (30–34)
  по мед.показ.:  +1 (медкомиссия от 12.01.2026)
  итоговая:       4 (35–39)
```

Это **идеально читаемо** и для вас, и для проверяющих.

---

## 2. Это НЕ категория ФП — и это критично

Вы правильно почувствовали грань.

* **Категория ФП (1/2/3)** — это требования по должности/назначению.
* **Возрастная группа** — это норматив сложности.

👉 Медики **НЕ меняют категорию ФП**,
👉 Медики **МОГУТ менять возрастную группу**.

Это правильная, нормативно чистая логика.

---

## 3. Квалификационный уровень — вы правы, его не хватало

Да, **блок расчёта квалификационного уровня (КУ) нужен**, и он **отличается от итоговой оценки**.

### Что такое квалификационный уровень (обобщённо)

КУ — это:

* оценка **по сумме и структуре результатов**,
* используется для:

  * допуска,
  * присвоения/подтверждения квалификации,
  * отчётности.

⚠️ В РФП-2015 он **не всегда обязателен**, но если у вас он используется — его надо выделять отдельно.

---

### Как правильно заложить в архитектуру

#### Разделяем:

* `final_grade` — итог за проверку (отлично/хорошо/удовл./неуд.)
* `qualification_level` — отдельный результат

```text
qualification_level:
  NONE
  LEVEL_3
  LEVEL_2
  LEVEL_1
  MASTER
```

(конкретный перечень — из ваших документов)

---

### Где считать КУ

Добавить **отдельный модуль**:

```text
IQualificationLevelEngine
  Calculate(pack, participant, results, policies) -> QualificationResult
```

И отдельный Norms Pack (или раздел в существующем):

```
appendix_qualification.json
```

Если сейчас **точных правил нет**:

* в ТЗ явно написать:

> Расчёт квалификационного уровня не выполняется, модуль зарезервирован.

Это лучше, чем “потом допилим”.

---

## 4. Причины не сдачи — фиксируем как ENUM + нормализация

Вы дали **идеальный практический список** 👍
Его **нужно стандартизировать**, но сохранить исходный ввод.

### Предлагаемый ENUM `participation_reason_code`

```text
BUSINESS_TRIP     (командировка)
DUTY              (наряд)
VACATION          (отпуск)
SICK_LEAVE        (больничный)
MEDICAL_EXEMPT    (освобождение)
LFK               (ЛФК)
NO_SHOW           (неприб.)
```

### В БД храним:

```text
participation_status        — enum (completed / no_show_valid / no_show_invalid / medical_exempt / lfk / refuse)
participation_reason_code  — enum (из списка выше)
participation_reason_text  — VARCHAR (как ввёл пользователь)
```

---

### Маппинг “по умолчанию”

| Код            | Статус          |
| -------------- | --------------- |
| BUSINESS_TRIP  | no_show_valid   |
| DUTY           | no_show_valid   |
| VACATION       | no_show_valid   |
| SICK_LEAVE     | no_show_valid   |
| MEDICAL_EXEMPT | medical_exempt  |
| LFK            | lfk             |
| NO_SHOW        | no_show_invalid |

⚠️ Пользователь может изменить статус вручную, но:

* изменение логируется;
* причина остаётся.

---

## 5. Что обязательно дописать в ТЗ (короткий чек-лист)

### Добавить сущности:

* `age_group_base`
* `age_group_med_delta`
* `age_group_effective`
* `age_group_med_reason`
* `age_group_med_source`

### Добавить политики:

* допустимый диапазон `med_delta` (например -1…+1)
* кто имеет право менять (роль)

### Добавить раздел:

* **Квалификационный уровень** (реализован / зарезервирован)

### Добавить ENUM:

* `participation_reason_code` (нормализованный)

---

## Итоговое решение (коротко)

✔ Да, **разрешаем менять возрастную группу по медпоказаниям**
✔ Но **только через delta**, с сохранением базового значения
✔ Категорию ФП медики **не трогают**
✔ Квалификационный уровень — **либо реализуем отдельным модулем, либо явно резервируем**
✔ Причины не сдачи — **нормализуем, но не теряем текст**
